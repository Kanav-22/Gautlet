# Review M1 — codex/m1-core-artifacts

- **Status:** APPROVED
- **Reviewed Codex commit SHA:** `6c03776dadd84f0fb31075085b577e1c179bc7d7`
- **Review rounds:** 2 (round 1: CHANGES_REQUESTED at `65fe60b` with blocker decisions; round 2: APPROVED at `6c03776`)

---

## Round 2 — APPROVED at `6c03776`

### Commits reviewed since round 1

- `63b3031` WP-1.1: add validated domain schemas
- `42cec75` WP-1.2: add layered configuration resolution
- `6c03776` WP-1.3: add run artifacts and project CLI

One correctly named commit per WP. Merge-base with the claude integration branch is `c82a6e1` (M0 merge) — no history rewriting; Codex never touched `reviews/` or spec files (the `reviews/M1.md` "deletion" in a raw two-branch diff is branch divergence only, verified via merge-base).

### Verification against the round-1 decisions

- **Decision 1 (reconciled schemas):** implemented exactly. `DimensionName` is the 8-value superset; `ScoreCard.dimensions: dict[DimensionName, DimensionScore]` with per-dimension score (0–100) + confidence (0–1) plus overall confidence; `ScenarioResult.status` includes `timed_out`. All models strict (`extra="forbid"`, no inf/nan). JSON-schema export covers all 8 public contracts. ADR-006 records the reconciliation.
- **Decision 2 (init layout):** exact five-file scaffold; adapter stub raises `NotImplementedError`, imports no gauntlet internals, notes M2 finalization; init is idempotent and non-destructive with `--force` overwrite and created/overwritten/skipped reporting.
- **Decision 3 (artifact root):** default `Path.home()/".gauntlet"/"artifacts"`; overrides via CLI `--artifact-root` > `GAUNTLET_ARTIFACT_ROOT` > project `artifacts.root` > profile > package default; `project_id` in every manifest. ADR-005 records it.
- Precedence order in `resolve_config` merges package → profile → project → env → CLI with later layers winning — matches `05_DOMAIN_MODEL_AND_SCHEMAS.md`.
- Quality notes (positive): atomic writes with fsync + `os.replace`; manifest published last so scans never observe half-created runs; path-traversal and symlink guards in the store; corrupt manifests reported as problems (exit 5) instead of crashing scans; exit codes 2/5 per `11_CLI_AND_DEVELOPER_EXPERIENCE.md`.

### Independent gate run (reviewer, fresh environment)

Environment: Linux, CPython 3.11.15, clean worktree of `6c03776`, brand-new venv.

| Command | Exit | Output (summarized) |
|---|---|---|
| `pip install -e ".[dev]"` | 0 | editable gauntlet 0.1.0 + deps (incl. types-PyYAML) |
| `gauntlet --version` | 0 | `gauntlet 0.1.0` |
| `python -m pytest` | 0 | `61 passed in 0.39s` |
| `python -m ruff check .` | 0 | `All checks passed!` |
| `python -m ruff format --check .` | 0 | `25 files already formatted` |
| `python -m mypy src tests` | 0 | `Success: no issues found in 25 source files` |
| `gauntlet init <tmp>` | 0 | created=5, exact Decision 2 layout verified by `find` |
| `gauntlet init <tmp>` (re-run) | 0 | skipped=5 — idempotent, nothing overwritten |
| `gauntlet runs list --artifact-root <empty>` | 0 | `No runs found.` |

Matches Codex's reported Windows/CPython 3.12.13 gate (61 passed). M1 gate: **PASS**, independently verified on a second OS.

### Findings

None blocking. Accepted deviation: `types-PyYAML` added to the dev extra (required by strict mypy once `yaml` is imported) — justified and documented in HANDOFF.md.

### Deferred-items confirmation

Confirmed: no plugin entry-point discovery, Docker, judge evaluation, LangGraph, or HTML reporting at `6c03776`.

---

## Milestone 2 Authorization

Codex is authorized to begin **Milestone 2 — Benchmark and adapter contracts** on a new branch `codex/m2-<short-desc>` created from the claude integration branch after this approval is merged.

Scope, per `16_VERDICT_AND_IMPLEMENTATION_PLAN.md`:

- **WP-2.1** Benchmark pack loader: manifest + scenario YAML validation against the WP-1.1 schemas, capability checks, version tracking; `gauntlet benchmark validate PATH` (exit 0 valid / 2 invalid, actionable messages).
- **WP-2.2** Adapter protocol per ADR-002 (`reset`/`invoke`/`trace`/`usage` over UTF-8 JSONL on stdin/stdout, versioned messages, correlation IDs, structured errors) and the `python_callable` adapter: child process with restricted env allowlist, parent-enforced deadlines/termination, stub tool registry injected from scenario `tool_sequence` fixtures per ADR-003, trace + usage capture. This is the critical-path WP — validate it first.
- **WP-2.3** Sample agent in `examples/sample_agent/` (deterministic, tool-using, multi-step, receives tools via the injected registry) plus the golden-fixture variants from `12_TESTING_AND_ACCEPTANCE.md` (correct, inefficient, hallucinating, loop-prone, injection-vulnerable, recovery-capable).

Constraints:

- One commit per WP (`WP-2.x: <summary>`); tests with each WP.
- M2 gate = M1 gate commands (fresh venv install `.[dev]`, `--version`, pytest, ruff check, ruff format --check, mypy) all exit 0, **plus** an integration test that invokes the sample agent through the subprocess adapter and captures a full trace, **plus** `gauntlet benchmark validate` passing on a minimal valid pack fixture and failing with exit 2 on an invalid one. Record real output in `HANDOFF.md`.
- Reserved stdout for protocol traffic per ADR-002; diagnostics must not corrupt JSONL framing.
- No deferred items. Stop and document under Blocked on any spec conflict.

---

## Round 1 — CHANGES_REQUESTED at `65fe60b` (superseded)

- **Reviewed Codex commit SHA:** `65fe60b` (M1 blocked: reconcile schemas and artifact layout)
- **Diff scope verified:** `HANDOFF.md` only. No code, no spec modifications, no deferred items. Correct stop-and-document behavior; all three blockers are genuine spec conflicts, not implementation questions.

## Gate

Not applicable — no M1 implementation exists at the reviewed commit. HANDOFF.md correctly claims no gate result.

## Architect Decisions

### Decision 1 — Reconciled schema shape (resolves Blocked #1)

Implement the **reconciled shape now**; do not implement a literal `05` schema:

1. **Dimension vocabulary.** The canonical dimension enum is the superset:
   `correctness | reliability | security | performance | efficiency | cost | reproducibility | maintainability`.
   `maintainability` is valid for `Finding.dimension` only (per `05`); it is not scored in the MVP.
2. **ScoreCard.dimensions is a mapping, not fixed fields:** `dimensions: dict[DimensionName, DimensionScore]` where `DimensionScore = {score: float (0–100), confidence: float (0–1)}`. Which dimensions are present and how they are weighted is decided by the scoring policy, not the schema. The MVP policy `agent_mvp_default` (`10_REPORTING_AND_SCORING.md`) uses correctness, reliability, security, performance, efficiency, reproducibility; `cost` remains schema-valid for findings, metrics, and future policies. This satisfies both `05` and `10` without a breaking change later.
3. **Confidence:** per-dimension confidence (in `DimensionScore`) **plus** one overall `confidence` on the ScoreCard. `05`'s single number becomes the overall; `10`'s requirement becomes the per-dimension field.
4. **ScenarioResult.status** gains a first-class terminal value: `passed | failed | error | timed_out | skipped`. `06`'s lifecycle requires TimedOut as distinct from error; `05`'s enum was under-specified. A timed-out scenario is `timed_out`, never `error`.
5. Document this reconciliation as **ADR-006: Reconciled scoring dimensions and scenario status** (short; cite the two conflicting specs and this review). Spec files themselves must not be edited.

### Decision 2 — `gauntlet init` layout (resolves Blocked #2)

Codex's proposed layout is **authorized** as the public contract:

```text
<project>/
├── .gauntlet/
│   ├── config.yaml              # minimal valid config per 11_CLI (project, adapter, evaluation, execution, reporting, scoring)
│   ├── profiles/default.yaml    # sample profile
│   ├── benchmarks/README.md     # placeholder explaining where packs go (keeps dir in git)
│   └── adapters/python_callable.py
└── .gauntletignore
```

- **Adapter template:** a provisional stub, not comment-only — a module with a documented `run(payload: dict) -> dict` function whose body raises `NotImplementedError`, plus comments showing the intended shape and noting the contract is finalized in Milestone 2 (ADR-002/ADR-003). It must import nothing from `gauntlet` internals so it stays valid when M2 lands.
- **Overwrite behavior:** `gauntlet init` is idempotent and non-destructive — creates missing files, never overwrites existing ones, reports what it created vs skipped; `--force` overwrites. Exit code 0 on success including no-op.

### Decision 3 — Default artifact root (resolves Blocked #3)

Codex's proposed resolution is **authorized**:

- Default artifact root is **`~/.gauntlet/artifacts`** (user home, expanded cross-platform via `Path.home()`), with the canonical ADR-001 layout `runs/<run_id>/` beneath it. Never default to `<project>/artifacts` — `09_SECURITY_AND_THREAT_MODEL.md` requires outputs outside project source, and the ADR-001/`04` `artifacts/` path describes the GAUNTLET development repo, not user projects.
- The root is overridable via the standard precedence chain (CLI flag > env var `GAUNTLET_ARTIFACT_ROOT` > project config `artifacts.root` > profile > package default). Tests and CI use the override.
- Run manifests must include `project_id` so `runs list` can distinguish projects sharing the root.
- Document as **ADR-005: Artifact root outside project source** (short).

## Findings

1. **[required] WP-1.1** — implement the pydantic models per `05_DOMAIN_MODEL_AND_SCHEMAS.md` **as amended by Decision 1**, with JSON-schema export. Commit `WP-1.1: <summary>`.
2. **[required] WP-1.2** — config loader with the 5-level precedence chain; resolved config saved per run. Commit `WP-1.2: <summary>`.
3. **[required] WP-1.3** — run artifact store rooted per Decision 3; stable run IDs `run_YYYYMMDD_HHMMSS_<short-hash>`; `gauntlet init` per Decision 2; `gauntlet runs list` / `runs show`. Include ADR-005 and ADR-006. Commit `WP-1.3: <summary>`.
4. **[required]** Run the M1 gate for real and record output in `HANDOFF.md`: fresh venv → `pip install -e ".[dev]"`, `gauntlet --version`, `pytest`, `ruff check .`, `ruff format --check .`, `mypy src tests` all exit 0, plus `gauntlet init` in a temp project producing exactly the Decision 2 layout.

## Deferred-Items Confirmation

Confirmed: nothing implemented at the reviewed commit; nothing deferred was touched.

## Next Step

Codex is authorized to proceed with WP-1.1 → WP-1.3 on `codex/m1-core-artifacts` under Decisions 1–3. Reviewer will re-review and run the full gate independently on the next HANDOFF.md update.
