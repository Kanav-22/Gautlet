# Review M4 — codex/m4-scoring-reports

- **Status:** APPROVED
- **Reviewed Codex commit SHA:** `7fa09a5c0996a4192d4729b919a756431f9c31c2`
- **Commits reviewed:** `87482b5` (WP-4.1: add evidence-linked metric collectors), `22d8db7` (WP-4.2: add policy-driven scoring engine), `cf24c10` (WP-4.3: add scored report pipeline), `7fa09a5` (WP-4.4: add context-aware run comparison)
- **Branch topology verified:** merge-base with the claude integration branch is `3cde977` (M3 merge) — correct base, no history rewriting. Diff adds `metrics/`, `scoring/`, `reporting/` plus small documented extensions to `evidence/` and `cli/app.py`. No spec files (00–16), no `reviews/`; `adapters/` and `execution/` untouched per the M4 constraint.

## Verification against the M4 authorization (reviews/M3.md)

- **WP-4.1:** collectors cover task_success, latency, steps, tool_calls, retries, recovery_steps, exceptions, and exact adapter-reported usage. **Token/cost figures are published only when every contributing attempt supplies valid counters — absent data stays absent, never zero-filled or estimated.** Verified in code and by dedicated tests.
- **WP-4.2:** `agent_mvp_default.yaml` reproduces the `10_REPORTING_AND_SCORING.md` example exactly (weights 0.30/0.25/0.20/0.10/0.10/0.05; caps `critical_security_finding: 49`, `task_success_below_50_percent: 59`; minimum `scenarios_completed: 10`). Caps apply after weighting; every applied rule is recorded as a `PolicyRuleApplication` citation. Reproducibility requires explicit repeat evidence (≥2 repeats) per ADR-004 — never inferred from a single run; missing repeat evidence lowers confidence toward `evaluation_inconclusive`.
- **WP-4.3:** pipeline publishes `results.json`, `scorecard.json`, `findings.json`, and `report.md` atomically with whole-run redaction before publication; **the completed manifest is written only after every required artifact succeeds** (verified by a dedicated failure-path test). Exit codes map per `11_CLI_AND_DEVELOPER_EXPERIENCE.md`.
- **WP-4.4:** `gauntlet compare` reports score/failure/latency/cost deltas with explicit comparability context; deterministic regressions require exact new-failure or score-decrease evidence; config, benchmark-version, and environment changes are reported independently and distinguished from regressions; live-service or missing-cost comparisons never fabricate conclusions.

### Accepted deviations (documented in HANDOFF.md)

Normalization formulas and recommendation bands (`ready_score: 80`, `passing_score: 60`) are recorded in the policy file rather than hidden in code — accepted, spec left them undefined. Compare exit codes (1 regression / 0 none-or-context-change / 2 invalid / 5 incomplete) — accepted, spec silent. Benchmark provenance + config fingerprint persisted in the report summary — accepted, needed for the `10` regression rules. Evidence-store extension for usage evidence and atomic Markdown — accepted with justification. Full `gauntlet evaluate` CLI remains M5 scope as authorized.

## Independent gate run (reviewer, fresh environment)

Environment: Linux, CPython 3.11.15, clean worktree of `7fa09a5`, brand-new venv.

| Check | Exit | Output (summarized) |
|---|---|---|
| `pip install -e ".[dev]"` / `gauntlet --version` | 0 | gauntlet 0.1.0 |
| `python -m pytest` | 0 | **203 passed in 9.57s** |
| M4 acceptance: correct golden agent outscores degraded + all reports written | 0 | passed |
| M4 acceptance: hand-computed scores match policy math | 0 | passed |
| M4 acceptance: degraded run flagged as regression | 0 | passed |
| M4 acceptance: config change distinguished from regression | 0 | passed |
| Inherited five security tests (M3) | 0 | 5 passed |
| `ruff check .` / `ruff format --check .` | 0 | clean / 64 files formatted |
| `mypy src tests` | 0 | no issues in 53 source files |
| named adapter integration test | 0 | 1 passed |
| `benchmark validate` valid / invalid | 0 / 2 | reproduced |

Matches Codex's Windows gate (203 passed; its HANDOFF honestly discloses a discarded first pytest attempt caused by sandbox tmp-path ACLs — accepted, environmental only). M4 gate: **PASS**, independently verified on a second OS.

## Findings

None blocking.

## Deferred-items confirmation

Confirmed: no Docker, judge evaluation, plugin entry-point discovery, LangGraph, or HTML reporting at `7fa09a5`.

---

## Milestone 5 Authorization

Codex is authorized to begin **Milestone 5 — Agent MVP pack and acceptance** on a new branch `codex/m5-<short-desc>` created from the claude integration branch after this approval is merged. This is the final MVP milestone.

Scope, per `16_VERDICT_AND_IMPLEMENTATION_PLAN.md`, `07_AGENT_EVALUATION_PACK.md`, `03_PRD.md`, and `12_TESTING_AND_ACCEPTANCE.md`:

- **WP-5.1** Author `benchmarks/agent_mvp/` — the flagship pack: manifest (`gauntlet.agent.mvp`, schema per `05`), all **15 MVP scenarios** from `07` (direct answer; single tool call; two-step dependent calls; unavailable tool; tool timeout; malformed tool response; contradictory instructions; malicious retrieved content; missing information; repeated failure/loop risk; state reset; long irrelevant context; forbidden tool attempt; hallucinated completion check; reproducibility across seeds) as scenario YAMLs with fixtures, plus `scoring.yaml` referencing `agent_mvp_default`. Scenarios must exercise the golden agent variants meaningfully (e.g., the injection scenario must fail the injection-vulnerable variant and pass the correct one).
- **WP-5.2** Complete the `gauntlet evaluate PATH` CLI (resolve config → validate environment → load pack → execute → collect evidence → score → write artifacts → generate reports → print report path; flags per `11`: `--profile`, `--benchmark`, `--scenario`, `--seed`, `--repeat`, `--offline`; exit codes per `11`), plus `gauntlet inspect PATH` (framework/entry-point detection for python-callable projects, warnings) and `gauntlet doctor` (environment checks).
- **WP-5.3** MVP acceptance: run the 10-point checklist from `12_TESTING_AND_ACCEPTANCE.md`, including **three deterministic runs with byte-identical canonical outputs** (per ADR-004's comparison set), a degraded-agent regression demonstration via `compare`, CI-style non-zero exit on policy failure, a committed example generated report (redacted), and `docs/quickstart.md` targeting the 15-minute first-evaluation goal.

Constraints:

- One commit per WP (`WP-5.x: <summary>`); tests with each WP.
- M5 gate = M4 gate commands all exit 0, **plus** the full 10-point MVP acceptance checklist with real output for each point recorded in `HANDOFF.md`, **plus** `gauntlet evaluate` on the sample agent completing end-to-end with a scorecard and Markdown report.
- No changes to `adapters/`, `execution/`, `scoring/`, or `reporting/` internals without documented justification.
- No deferred items. Stop and document under Blocked on any spec conflict.
