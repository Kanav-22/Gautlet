# Review M0 — codex/m0-skeleton

- **Status:** APPROVED
- **Reviewed Codex commit SHA:** `4a3e4487101e6909631f827c1fde4499d37367b0`
- **Review rounds:** 2 (round 1: CHANGES_REQUESTED at `aaf2ee0`; round 2: APPROVED at `4a3e448`)

---

## Round 2 — APPROVED at `4a3e448`

### Commits reviewed since round 1

- `4a3e448` (WP-0.2: scaffold package and CLI) — the only new commit; correctly named, one commit per WP.

### Diff scope verified

`.gitignore`, `pyproject.toml`, `src/gauntlet/**` (11 architecture packages: cli, config, core, discovery, execution, evidence, metrics, scoring, reporting, plugins, adapters — matches `04_SYSTEM_ARCHITECTURE.md`), `tests/test_cli.py`, `HANDOFF.md`. No spec files (00–16) modified. No deferred items: no public plugin entry-point discovery, no Docker, no judge evaluation, no LangGraph, no HTML reporting. `[project.scripts]` is the CLI console script, not plugin discovery.

### WP-0.2 assessment

- `pyproject.toml`: Python `>=3.11`; runtime deps typer/pydantic/PyYAML/rich with sane bounds; `dev` optional group with pytest/ruff/mypy exactly per the round-1 gate decision; hatchling build; single version source in `__about__.py`.
- CLI: Typer app with eager `--version` callback; `python -m gauntlet` entry; `no_args_is_help`.
- Tests: version option output and package-vs-distribution version consistency; both meaningful for a skeleton.
- Tooling: ruff (lint + format) and strict mypy configured and passing.
- `HANDOFF.md`: honest, complete gate transcript (Windows, CPython 3.12.13), deviations correctly cite the round-1 authorization for `.[dev]`, Blocked section empty.

### Independent gate run (reviewer, fresh environment)

Environment: Linux, CPython 3.11.15, clean worktree of `4a3e448`, brand-new venv.

| Command | Exit | Output (summarized) |
|---|---|---|
| `python3 -m venv <fresh>` | 0 | created |
| `pip install -e ".[dev]"` | 0 | gauntlet 0.1.0 editable + typer 0.26.8, pydantic 2.13.4, PyYAML 6.0.3, rich 14.3.4, pytest 9.1.1, ruff 0.15.21, mypy 1.20.2 |
| `gauntlet --version` | 0 | `gauntlet 0.1.0` |
| `python -m pytest` | 0 | `2 passed in 0.03s` |
| `python -m ruff check .` | 0 | `All checks passed!` |
| `python -m ruff format --check .` | 0 | `16 files already formatted` |
| `python -m mypy src tests` | 0 | `Success: no issues found in 16 source files` |

Reviewer results match Codex's reported gate on a different OS and Python version (Windows/3.12 vs Linux/3.11). M0 gate: **PASS**, independently verified.

### Findings

None blocking. One non-blocking note for a future cleanup commit (do not block M1 on it):

1. **[nit] `pyproject.toml` `readme = "00_README.md"`** — the spec README doubles as the package readme; acceptable now, revisit when the project gets a proper top-level README.

### Deferred-items confirmation

Confirmed: no deferred items implemented at `4a3e448`.

---

## Milestone 1 Authorization

Codex is authorized to begin **Milestone 1 — Config, schemas, run artifacts** on a new branch named `codex/m1-<short-desc>`, created from the claude integration branch (`claude/repo-folder-review-plan-ombqwq`) after this approval is merged, which includes the approved M0 work.

Scope, per `16_VERDICT_AND_IMPLEMENTATION_PLAN.md`:

- **WP-1.1** Pydantic models for every entity in `05_DOMAIN_MODEL_AND_SCHEMAS.md` (EvaluationRun, Scenario, ScenarioResult, Evidence, Finding, ScoreCard) + benchmark pack manifest + config models, with JSON-schema export.
- **WP-1.2** Config loader implementing the 5-level precedence chain (CLI flags > env vars > project config > profile defaults > package defaults); resolved config saved per run.
- **WP-1.3** Run artifact store implementing `artifacts/runs/<run_id>/` per the domain model; stable run IDs (`run_YYYYMMDD_HHMMSS_<short-hash>`); `gauntlet init`, `gauntlet runs list`, `gauntlet runs show`.

Constraints:

- One commit per WP (`WP-1.x: <summary>`); unit tests with each WP (schema round-trips, precedence order, artifact layout).
- M1 gate = fresh venv `pip install -e ".[dev]"`, `gauntlet --version`, `pytest`, plus `ruff check`, `ruff format --check`, `mypy src tests` all exit 0, plus `gauntlet init` producing the documented `.gauntlet/` layout (`03_PRD.md` FR-001). Record real output in `HANDOFF.md`.
- Files-only persistence per ADR-001. No deferred items. Stop and document in `HANDOFF.md` under Blocked on any spec conflict.

---

## Round 1 — CHANGES_REQUESTED at `aaf2ee0` (superseded)

- **Reviewed SHA:** `aaf2ee09f59bfc788fb7af28c628b6592a2c4865`
- WP-0.1 (ADR-001..004) accepted as-is: template-conformant, matched the plan's decision table, honest claims (ADR-002 disclaims sandbox status; ADR-004 excludes run-identity metadata from byte-identical comparison). Diff touched only `docs/adr/` + `HANDOFF.md`.
- Gate not run (no package existed); HANDOFF.md correctly claimed no results.
- **Architect decision on the raised blocker:** gate install step is `pip install -e ".[dev]"`; pytest/ruff/mypy remain a `dev` optional-dependency group. Runtime installs must not receive test tooling. Applies to all subsequent milestone gates.
- Changes requested solely because WP-0.2 was outstanding. Resolved in round 2.
